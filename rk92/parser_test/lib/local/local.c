#include "local.h"
#include <stdio.h>


/*int setLocalCallback(void* newCallbackP) {
    if(newCallbackP) {
        callbackp = newCallbackP;
    } else {
        status =-1;
        (*callbackp)();
    }
}

void defaultCallback() {
    printf("Status: %d\n",status);
}
*/